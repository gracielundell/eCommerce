import { productComments } from 'e-commerce/helpers/product-comments';
import { module, test } from 'qunit';

module('Unit | Helper | product comments');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productComments([42]);
  assert.ok(result);
});
